import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.retrogamecollection.app',
  appName: 'Retro Game Collection',
  webDir: 'dist'
};

export default config;
